<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home | Cazzola Propiedades</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body class="d-flex flex-column min-vh-100">
    <%- include('partials/header') %>
    <main class="container mb-5">
      <section class="py-4 text-center">
        <p class="text-uppercase text-muted mb-1">Cazzola Propiedades</p>
        <h1 class="fw-bold">Imobiliária moderna para venda e aluguel</h1>
        <p class="lead text-muted">Busque, visite e feche negócios com suporte especializado.</p>
        <div class="d-flex justify-content-center gap-2 mt-3">
          <a class="btn btn-primary" href="/imoveis">Ver imóveis</a>
          <a class="btn btn-outline-primary" href="/contato">Quero contato</a>
        </div>
      </section>

      <section class="py-4">
        <div class="d-flex align-items-center justify-content-between mb-3">
          <h2 class="h4 mb-0">Destaques recentes</h2>
          <a class="btn btn-link" href="/imoveis">Ver todos</a>
        </div>
        <div class="row g-3">
          <% if (featured && featured.length) { %>
            <% featured.forEach((property) => { %>
              <div class="col-md-4">
                <article class="card h-100">
                  <div class="ratio ratio-16x9 bg-light">
                    <img
                      src="<%= property.images && property.images[0] ? property.images[0] : 'https://via.placeholder.com/800x450' %>"
                      class="card-img-top object-fit-cover"
                      alt="Foto do imóvel"
                    />
                  </div>
                  <div class="card-body">
                    <p class="small text-uppercase text-muted mb-1"><%= property.type %> • <%= property.status %></p>
                    <h3 class="h5"><%= property.title %></h3>
                    <p class="fw-semibold text-primary">US$ <%= property.price %></p>
                    <p class="text-muted small mb-2"><%= formatLocation(property) %></p>
                    <a class="btn btn-outline-primary btn-sm" href="/imoveis/<%= property._id %>">Ver detalhes</a>
                  </div>
                </article>
              </div>
            <% }); %>
          <% } else { %>
            <p class="text-muted">Cadastre imóveis para vê-los aqui.</p>
          <% } %>
        </div>
      </section>
    </main>
    <%- include('partials/footer') %>
  </body>
</html>
