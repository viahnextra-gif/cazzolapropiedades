<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Imóveis | Cazzola Propiedades</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body class="d-flex flex-column min-vh-100">
    <%- include('partials/header') %>
    <main class="container mb-5">
      <div class="d-flex align-items-center justify-content-between mb-3">
        <div>
          <p class="text-uppercase text-muted mb-1">Catálogo</p>
          <h1 class="h3 mb-0">Imóveis para venda e aluguel</h1>
        </div>
        <a class="btn btn-primary" href="/admin/properties/new">+ Novo imóvel</a>
      </div>

      <section class="card mb-4">
        <div class="card-body">
          <form class="row g-3" method="get" action="/imoveis">
            <div class="col-md-3">
              <label class="form-label">Tipo</label>
              <select name="type" class="form-select">
                <option value="">Todos</option>
                <option value="venda" <%= filters.type === 'venda' ? 'selected' : '' %>>Venda</option>
                <option value="aluguel" <%= filters.type === 'aluguel' ? 'selected' : '' %>>Aluguel</option>
              </select>
            </div>
            <div class="col-md-3">
              <label class="form-label">Localização</label>
              <input type="text" name="location" value="<%= filters.location || '' %>" class="form-control" placeholder="Bairro ou cidade" />
            </div>
            <div class="col-md-3">
              <label class="form-label">Preço mínimo</label>
              <input type="number" name="minPrice" value="<%= filters.minPrice || '' %>" class="form-control" />
            </div>
            <div class="col-md-3">
              <label class="form-label">Preço máximo</label>
              <input type="number" name="maxPrice" value="<%= filters.maxPrice || '' %>" class="form-control" />
            </div>
            <div class="col-md-3">
              <label class="form-label">Quartos</label>
              <input type="number" name="bedrooms" value="<%= filters.bedrooms || '' %>" class="form-control" />
            </div>
            <div class="col-md-3">
              <label class="form-label">Status</label>
              <select name="status" class="form-select">
                <option value="">Todos</option>
                <option value="ativo" <%= filters.status === 'ativo' ? 'selected' : '' %>>Ativo</option>
                <option value="inativo" <%= filters.status === 'inativo' ? 'selected' : '' %>>Inativo</option>
              </select>
            </div>
            <div class="col-md-12 d-flex justify-content-end gap-2 mt-2">
              <a class="btn btn-outline-secondary" href="/imoveis">Limpar</a>
              <button type="submit" class="btn btn-primary">Filtrar</button>
            </div>
          </form>
        </div>
      </section>

      <section class="row g-3">
        <% if (properties && properties.length) { %>
          <% properties.forEach((property) => { %>
            <div class="col-md-4">
              <article class="card h-100">
                <div class="ratio ratio-16x9 bg-light">
                  <img
                    src="<%= property.images && property.images[0] ? property.images[0] : 'https://via.placeholder.com/800x450' %>"
                    class="card-img-top object-fit-cover"
                    alt="Foto do imóvel"
                  />
                </div>
                <div class="card-body d-flex flex-column">
                  <p class="small text-uppercase text-muted mb-1"><%= property.type %> • <%= property.status %></p>
                  <h2 class="h5 mb-1"><%= property.title %></h2>
                  <p class="fw-semibold text-primary">US$ <%= property.price %></p>
                  <p class="text-muted small mb-3"><%= formatLocation(property) %></p>
                  <div class="mt-auto d-flex gap-2">
                    <a class="btn btn-outline-primary btn-sm" href="/imoveis/<%= property._id %>">Ver detalhes</a>
                    <a class="btn btn-outline-secondary btn-sm" href="mailto:contato@cazzola.com?subject=Interesse%20no%20im%C3%B3vel%20<%= property.title %>">Contatar</a>
                  </div>
                </div>
              </article>
            </div>
          <% }); %>
        <% } else { %>
          <p class="text-muted">Nenhum imóvel encontrado. Ajuste os filtros ou cadastre um novo.</p>
        <% } %>
      </section>
    </main>
    <%- include('partials/footer') %>
  </body>
</html>
